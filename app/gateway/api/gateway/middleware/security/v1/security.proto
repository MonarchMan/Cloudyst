syntax = "proto3";
package gateway.middleware.security.v1;
option go_package = "gateway/api/gateway/middleware/security/v1";

// Security middleware config.
message Security {
    optional string cache_control = 1;
    repeated string white_list = 2;
}
